import { OperatorSystemShell } from "../OperatorSystemShell";

export function HunterKillerScanner() {
  return (
    <OperatorSystemShell
      codename="SYS-03 // HUNTER-KILLER SCANNER"
      description="Active scanning module for identifying and flagging exposed or vulnerable targets."
    />
  );
}
