import { OperatorSystemShell } from "../OperatorSystemShell";

export function CloudflarePerimeterConsole() {
  return (
    <OperatorSystemShell
      codename="SYS-12 // CLOUDFLARE PERIMETER CONSOLE"
      description="Console for the Cloudflare-facing perimeter — Worker routes, WAF rules, and edge configuration."
    />
  );
}
