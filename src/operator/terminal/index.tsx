import { OperatorSystemShell } from "../OperatorSystemShell";

export function CodexTerminal() {
  return (
    <OperatorSystemShell
      codename="SYS-06 // CODEX TERMINAL"
      description="Direct command-line style interface into the harness, for operators who'd rather type than click."
    />
  );
}
