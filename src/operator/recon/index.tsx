import { OperatorSystemShell } from "../OperatorSystemShell";

export function ReconSuite() {
  return (
    <OperatorSystemShell
      codename="SYS-01 // RECON SUITE"
      description="Forward intel gathering surface for the harness — target scoping and signal collection before any builder work starts."
    />
  );
}
