import { OperatorSystemShell } from "../OperatorSystemShell";

export function SignalIntelligencePanel() {
  return (
    <OperatorSystemShell
      codename="SYS-02 // SIGNAL INTELLIGENCE"
      description="Live signal intake and triage — surfaces inbound signal from monitored sources before it reaches Recon."
    />
  );
}
