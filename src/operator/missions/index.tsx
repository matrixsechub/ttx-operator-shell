import { OperatorSystemShell } from "../OperatorSystemShell";

export function MissionComposer() {
  return (
    <OperatorSystemShell
      codename="SYS-08 // MISSION COMPOSER"
      description="Drafts and assembles multi-step missions before they're dispatched to the pipeline."
    />
  );
}
