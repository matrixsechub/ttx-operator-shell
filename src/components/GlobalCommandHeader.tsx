/**
 * RECONSTRUCTED PLACEHOLDER — the original GlobalCommandHeader was imported
 * by OperatorShell but never committed to this branch. Renders nothing so
 * the shell keeps its exact layout minus the missing header strip. Replace
 * with the original implementation when it is recovered.
 */
export function GlobalCommandHeader() {
  return null;
}
