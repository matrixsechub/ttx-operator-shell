export function InterventionPanel({ disabled, busy, onAction }: { disabled: boolean; busy: boolean; onAction: (action: "pause" | "lower" | "safe-mode" | "terminate" | "evidence") => void }) {
  const actions = [["pause", "Pause execution"], ["lower", "Lower autonomy"], ["safe-mode", "Enter safe mode"], ["terminate", "Terminate mission"], ["evidence", "Request evidence"]] as const;
  return <section className="op-panel-raised rounded-sm p-4" aria-labelledby="flywheel-interventions-title"><h2 id="flywheel-interventions-title" className="text-xs font-semibold uppercase tracking-widest text-op-text">Quick interventions</h2><div className="mt-3 flex flex-wrap gap-2">{actions.map(([action, label]) => <button key={action} type="button" disabled={disabled || busy} onClick={() => onAction(action)} className="rounded-sm border border-op-border px-3 py-2 text-xs font-semibold text-op-text transition hover:border-op-accent focus-visible:outline focus-visible:outline-2 focus-visible:outline-op-accent disabled:cursor-not-allowed disabled:opacity-40">{label}</button>)}</div></section>;
}
