node_modules/
dist/
dist-app-tmp/
.wrangler/
worker-configuration.d.ts
*.tsbuildinfo
.env
.env.*
!.env.example

# Local wrangler dev secrets (auth credentials) — never commit
.dev.vars
.dev.vars.*

# Local machine config, not project config (e.g. Windows script-shell overrides)
.npmrc

# Local evidence, dry-runs, and operator artifacts — never commit
artifacts/
.artifacts/
dist-dryrun/
dist-staging-dryrun/
*.log
actions-runner-*.zip
