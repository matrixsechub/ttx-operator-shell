node_modules/
dist/
.wrangler/
worker-configuration.d.ts
*.tsbuildinfo
.env
.env.*
!.env.example

# Local wrangler dev secrets (auth credentials) — never commit
.dev.vars
.dev.vars.*

# PRISM Phase 2A browser capture artifacts (generated, may contain screenshots)
artifacts/uiux/

# Playwright output
playwright-report/
test-results/

# Local machine config, not project config (e.g. Windows script-shell overrides)
.npmrc

# Local Cursor IDE settings (plugin toggles / workstation preferences)
# Shared agent prompts under .cursor/agents/ remain trackable.
.cursor/settings.json
